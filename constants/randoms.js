const randomIndexIntervals = {
    'MIN': 0,
    'MAX': 7,
};

const randomBitStates = {
    'NEGATIVE': 0,
    'POSITIVE': 1
};

const RANDOM_BIT_INCREMENTER = 1;
const randomIntegerIndeces = {
    'MIN': 0,
    'PRIMARY': 1,
    'SECONDARY': 2,
    'MAX': 3,
    'RANGE_UPPER_BOUND': 4,
};

module.exports = { RANDOM_BIT_INCREMENTER, randomIndexIntervals, randomBitStates, randomIntegerIndeces }